package sdo.products.test.unit

import org.specs2.mutable.Specification
import scalaz.Scalaz._
import sdo.core.domain._
import sdo.peopleAnOrganizations.domain._

class ProductFeatureSpecs extends Specification {

	"A product can have features that" should {
		
		"be defined by the interaction between  a feature and a product" in {
			pending
		}
		
		"be applicable (ie required, standard, optional, selectable)" in {
			pending
		}
		
		"be applicable only for a period of time" in {
			pending
		}
		
		"optionally have a unit of measure" in {
			pending
		}
	}
	
}